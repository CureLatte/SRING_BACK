export type SignInfoDto = {
	name: string;
	profile: string;
};

export type LoginToken = {
	accessToken: string;
	refreshToken: string;
};

export type UserProfileInfo = {
	profile: string;
	name: string;
	nickName: string;
};
